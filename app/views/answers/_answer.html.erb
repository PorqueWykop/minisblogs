<%= form_for([tweet, Answer.new], remote: true) do |f| %>
  		<div class="field">
    		<%= f.label :Wiadomosc %><br />
    		<%= f.text_field :status %>
  		</div>
  		<div class="actions" onclick="showHide('answers');">
    		<%= f.submit("Odpowiedz") %>
  		</div>
<% end %>